# rhds

## To recreate the same environment, run:

```bash
git clone <github URL>
cd rhds
mamba env create -f environment.yml
mamba activate rhds
Rscript install.r
```

## Set up your config.env using config-template.env and replace the placeholder paths with your specific paths
